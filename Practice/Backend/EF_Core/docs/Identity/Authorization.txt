Authorization
 - Determining wether a user has access to a resource or not.
 - Authorization verifies what the user is allowed to do.

 - Not ALL authenticated users might have access to to parts of an application. 
   For example, Admin panels should not be accessible to regular users.

 - Authorization is independent and happens after authentication. However, authorization
   requires an authentication mechanism.
   
 
